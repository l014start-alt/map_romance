/* 낭만 별자리 지도(2D 데모) 미리보기 라우트 — /brandmap
   영상의 인터랙션(노드 연결 + 상세 패널 + 게이지)을 우리 밝은 톤·2D로 재해석.
   실제 콘텐츠/데이터 확정되면 이 데모를 기준으로 다듬으면 됩니다. */
import ConstellationMap from '@/components/ConstellationMap'

export default function BrandMapPage() {
  return <ConstellationMap />
}
